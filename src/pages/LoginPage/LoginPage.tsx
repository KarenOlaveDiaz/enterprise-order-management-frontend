export function LoginPage() {
    return (
      <section>
        <h1>Login</h1>
  
        <p>
          Authentication will be connected to the NestJS backend.
        </p>
      </section>
    );
  }